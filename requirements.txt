# ============================================================
# Dashboard BPO Financeiro — Dependências Python
# ============================================================
# Instalação: pip install -r requirements.txt

streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.20.0
google-generativeai>=0.7.0
tabulate>=0.9.0        # necessário para df.to_markdown()
openpyxl>=3.1.0        # leitura de arquivos .xlsx (opcional)
python-dotenv>=1.0.0   # gerenciamento de variáveis de ambiente (opcional)
